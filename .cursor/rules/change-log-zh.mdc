---
description: 修改并上传后须写中文变更日志
alwaysApply: true
---

# 修改上传与中文日志

每次完成代码、配置或场景修改，并在用户要求或任务需要**提交/推送（上传）**后，必须补充**中文变更日志**。

## 何时写

| 时机 | 要求 |
|---|---|
| 任务结束、已向仓库提交 | 写中文变更日志 |
| 已向远程推送 | 在日志中注明已推送 |
| 仅本地修改、未上传 | 在回复末尾用中文简要说明改了什么 |

## 日志内容（中文）

按条列出，每条说清「改了什么」和「为什么」：

1. **变更摘要**：1～3 句话概括本次目的与结果
2. **改动范围**：涉及模块/文件（不必逐行罗列）
3. **行为变化**：玩家或系统侧可见的差异（无则写「无」）
4. **验证**：已跑过的检查或待测项

## 提交信息

用户要求 commit / push 时：

- **commit message 使用中文**，说明动机而非只列文件名
- 推送后在变更日志中写一句「已推送到 `<分支名>`」

## 示例

```text
## 变更日志

**摘要**：修复历练事件结算后背包未同步的问题。

**改动范围**：`lilian_settle_service.gd`、`GameState.settle_lilian()`

**行为变化**：历练结算后背包物品与存档一致。

**验证**：本地跑通一次历练结算；待测：读档后背包仍正确。

**提交**：已提交并推送到 `fix/lilian-inventory-sync`
```

## 禁止

- 只用英文 commit message（除非用户明确要求英文）
- 上传后不写任何说明就结束对话
- 日志只写「已修改」「已完成」等空话，不说明实际变更
