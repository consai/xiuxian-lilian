---
description: UI 场景布局与可复用场景规范
globs: "**/*.tscn"
alwaysApply: true
---

# UI 场景布局与复用规范

生成或修改 `.tscn` 时，优先使用场景组合完成布局，不在代码中动态创建固定 UI 节点。

## 复用顺序

1. 优先检查并复用 `res://scenes/items/` 中已有场景，例如物品格、技能格、背包容器。
2. 若 `res://scenes/items/` 中没有，但功能点可被多个界面复用，应创建新的独立 `.tscn` 场景，再由主场景实例化。
3. 主场景只负责组合与定位，避免重复定义相同功能节点。

## 脚本边界

- 固定 UI 布局写在 `.tscn` 中。
- 脚本只负责数据绑定、状态切换和信号处理。
- 原型阶段要求 TODO 管理代码时，函数只保留接口、信号和 TODO 注释，不实现完整业务逻辑。
