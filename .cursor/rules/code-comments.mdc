---
description: 新增代码必须添加注释，说明意图与关键逻辑
alwaysApply: true
---

# 新增代码注释规范

编写或修改代码时，**新增的函数、分支、常量、配置项**必须附带注释，便于后续维护与策划/同事理解。

## 必须注释的内容

| 类型 | 要求 |
|------|------|
| 新增函数/方法 | 说明用途、入参含义、返回值或副作用 |
| 非显而易见的分支 | 说明触发条件与业务原因（如 `if`、`switch`、提前 `return`） |
| 魔法数字/字符串 | 说明含义或来源（配置 ID、协议码、状态码等） |
| 复杂算法或状态机 | 简述步骤或状态流转 |
| 与引擎/脚本交互边界 | 说明调用时机、依赖的 C++/Lua API |

## 不必注释的内容

- 变量名、函数名已能自解释的 trivial 代码（如 `i = i + 1`）
- 纯机械性的 getter/setter、简单转发
- 仅为满足本规则而写的废话注释（如 `// 循环`）

## 注释风格

- **Lua**：用 `--`；函数上方用块注释，行内逻辑用行尾或上一行简短说明
- **C++**：用 `//`；公开接口可用简短 Doxygen 风格（`///` 或 `/** */`）
- 注释写**意图与原因**，少复述代码字面意思
- 中文或英文均可，与同文件现有风格保持一致

## 示例

```lua
-- 宋辽副本：根据当前波次刷新对应阵营 NPC，wave 从 1 开始
function RefreshWaveNPC(sceneId, wave)
    local npcId = WAVE_NPC_TABLE[wave]  -- 波次与 NPC 配置表映射
    if not npcId then
        return  -- 超出配置波次，不再刷新
    end
    ...
end
```

```cpp
// 宋辽战场：同步双方积分到客户端，仅在积分变化时推送
lianxu SyncCampScore(Scene* pScene, int campA, int campB)
{
    ...
}
```

## 原则

1. **新增必注**：本次 diff 中新增的代码块，默认应带注释；改旧代码时，若触及复杂逻辑，顺手补全缺失注释。
2. **改行为必更注**：逻辑变更时同步更新或删除过时注释，避免误导。
3. **注释即文档**：策划可调参数、副本阶段、活动规则等，注释应能让非作者快速理解「为什么这样写」。
