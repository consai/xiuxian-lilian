# 2026-07-09

## stackable 字段 bool → int 迁移
- `item_items.json`: 41 个 `true→1`, 6 个 `false→0`
- `item_generated_learning_books.json`: 2 个 `true→1`
- `item_def.gd`: 类型+默认值+判断逻辑修改
- `json_loader.gd`: `_build_generated_learning_book` 解析修改
- `item_info_payload_builder.gd`: 判断条件修改

## EnumPlayerAttr 全网应用

新建 `scripts/enum/enum_player_attr.gd` (class_name EnumPlayerAttr)，汇总所有玩家属性键（四维根基4 + 资质4 + 灵根元素5 + 战斗面板16 + 伤害类型3），每行带中文注释。

源文件委托：
- `zhandou_attr.gd`: 所有 const 改为 `EnumPlayerAttr.XXX`，方法保留
- `character_stats.gd`: 所有 const 改为 `EnumPlayerAttr.XXX`（LEGACY_FOUNDATION_KEYS 方向不同保留本地定义），方法保留
- `realm_balance_service.gd`: DEFAULT_ATTRIBUTE_FORMULA 字典 key + 函数体内引用 → EnumPlayerAttr

消费者文件 25 个，共 281 处替换：
- ZhandouAttr.XXX 常量 → EnumPlayerAttr.XXX（ZhandouAttr.ALL_KEYS → ALL_COMBAT_KEYS, TEST_DEFAULTS → COMBAT_TEST_DEFAULTS, CORE_KEYS → COMBAT_CORE_KEYS）
- CharacterStats.XXX 常量 → EnumPlayerAttr.XXX（仅常量，方法调用保留）
- ZhandouAttrScript.XXX / CharacterStatsScript.XXX preload 变量常量引用 → EnumPlayerAttr.XXX（preload 仅方法用时保留）
- ZhandouAttr.get_attr() 等所有方法调用不变
